# 🚨 مهم جداً: قم بنسخ محتوى هذا الملف إلى ملف جديد اسمه .env

# تعليمات:
# 1. افتح مجلد backend
# 2. أنشئ ملف جديد اسمه .env (بدون امتداد)
# 3. انسخ كل المحتوى التالي فيه
# 4. عدّل DATABASE_URL إذا كانت معلومات PostgreSQL مختلفة

PORT=5000
NODE_ENV=development

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/spatial_social_network

JWT_SECRET=spatial_social_network_secret_key_2024_change_in_production

MAPBOX_TOKEN=pk.YOUR_MAPBOX_TOKEN_HERE

CLIENT_URL=http://localhost:5173
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads
